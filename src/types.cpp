#include "types.h"

namespace litehtml
{
    const tstring style_display_strings =		_t("none;block;inline;inline-block;inline-table;list-item;table;table-caption;table-cell;table-column;table-column-group;table-footer-group;table-header-group;table-row;table-row-group");
    const tstring font_size_strings =		_t("xx-small;x-small;small;medium;large;x-large;xx-large;smaller;larger");
    const tstring font_style_strings =		_t("normal;italic");
    const tstring font_variant_strings =		_t("normal;small-caps");
    const tstring font_weight_strings =	_t("normal;bold;bolder;lighter100;200;300;400;500;600;700");
    const tstring list_style_type_strings =	_t("none;circle;disc;square;armenian;cjk-ideographic;decimal;decimal-leading-zero;georgian;hebrew;hiragana;hiragana-iroha;katakana;katakana-iroha;lower-alpha;lower-greek;lower-latin;lower-roman;upper-alpha;upper-latin;upper-roman");
    const tstring list_style_position_strings =	_t("inside;outside");
    const tstring vertical_align_strings =	_t("baseline;sub;super;top;text-top;middle;bottom;text-bottom");
    const tstring border_width_strings =	_t("thin;medium;thick");
    const tstring border_style_strings =	_t("none;hidden;dotted;dashed;solid;double;groove;ridge;inset;outset");
    const tstring element_float_strings =	_t("none;left;right");
    const tstring element_clear_strings =	_t("none;left;right;both");
    const tstring css_units_strings =	_t("none;%;in;cm;mm;em;ex;pt;pc;px;dpi;dpcm;vw;vh;vmin;vmax");
    const tstring background_attachment_strings =	_t("scroll;fixed");
    const tstring background_repeat_strings =	_t("repeat;repeat-x;repeat-y;no-repeat");
    const tstring background_box_strings =	_t("border-box;padding-box;content-box");
    const tstring element_position_strings =	_t("static;relative;absolute;fixed");
    const tstring text_align_strings =		_t("left;right;center;justify");
    const tstring text_transform_strings =		_t("none;capitalize;uppercase;lowercase");
    const tstring white_space_strings =		_t("normal;nowrap;pre;pre-line;pre-wrap");
    const tstring overflow_strings =		_t("visible;hidden;scroll;auto;no-display;no-content");
    const tstring background_size_strings =		_t("auto;cover;contain");
    const tstring visibility_strings =			_t("visible;hidden;collapse");
    const tstring border_collapse_strings =		_t("collapse;separate");
    const tstring pseudo_class_strings =		_t("only-child;only-of-type;first-child;first-of-type;last-child;last-of-type;nth-child;nth-of-type;nth-last-child;nth-last-of-type;not;lang");
    const tstring content_property_string =		_t("none;normal;open-quote;close-quote;no-open-quote;no-close-quote");
    const tstring media_orientation_strings =		_t("portrait;landscape");
    const tstring media_feature_strings =		_t("none;width;min-width;max-width;height;min-height;max-height;device-width;min-device-width;max-device-width;device-height;min-device-height;max-device-height;orientation;aspect-ratio;min-aspect-ratio;max-aspect-ratio;device-aspect-ratio;min-device-aspect-ratio;max-device-aspect-ratio;color;min-color;max-color;color-index;min-color-index;max-color-index;monochrome;min-monochrome;max-monochrome;resolution;min-resolution;max-resolution");
    const tstring box_sizing_strings =		_t("content-box;border-box");
    const tstring pointer_events_strings =		_t("auto;none;visiblePainted;visibleFill;visibleStroke;visible;painted;fill;stroke;all");
    const tstring media_type_strings =		_t("none;all;screen;print;braille;embossed;handheld;projection;speech;tty;tv");
}

