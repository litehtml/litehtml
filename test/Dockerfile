FROM ubuntu:24.04
LABEL authors="tordex"

RUN apt update
ENV DEBIAN_FRONTEND=noninteractive
RUN apt install -yq --no-install-recommends --no-install-suggests bash-completion build-essential cmake pkg-config libcairo2-dev libpango1.0-dev libgtk-3-dev
